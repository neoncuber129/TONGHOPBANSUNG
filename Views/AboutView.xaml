<UserControl x:Class="Tonghopbansung.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700">
    <Grid Margin="24" MaxWidth="560" HorizontalAlignment="Left">
        <Border Style="{StaticResource CardBorder}" Padding="28">
            <StackPanel>
                <TextBlock Text="GIỚI THIỆU" Style="{StaticResource SectionTitle}" FontSize="16" Margin="0,0,0,16"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Border Width="64" Height="64" Margin="0,0,16,0" CornerRadius="32" ClipToBounds="True">
                        <Image Source="pack://application:,,,/ico.jpg"
                               Stretch="UniformToFill"
                               RenderOptions.BitmapScalingMode="Fant"
                               SnapsToDevicePixels="True"
                               UseLayoutRounding="True"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Tổng hợp điểm bắn súng" FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Ứng dụng quản lý danh sách, nhập điểm và xếp loại"
                                   Opacity="0.75" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Author" Opacity="0.7" Margin="0,0,0,8"/>
                    <TextBlock Grid.Column="1" Text="TM" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Text="Nền tảng" Opacity="0.7"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="WPF · .NET"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
